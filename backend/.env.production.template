# DATABASE CONFIGURATION
# Generate a MongoDB connection string from your Hostinger MongoDB section or MongoDB Atlas
DATABASE=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/brickflow?retryWrites=true&w=majority

# PORT
# Hostinger might override this, but standard is 8080 or 8888
PORT=8888

# SECURITY
# Replace these with long, random strings for production security
SECRET=change_this_to_a_very_long_random_string_12345
JWT_SECRET=change_this_to_another_long_random_string_67890

# OPTIONAL
# OPENAI_API_KEY=your_openai_key_here
NODE_ENV=production
